sed -i '1i import kotlinx.coroutines.launch\nimport kotlinx.coroutines.GlobalScope\nimport kotlinx.coroutines.Dispatchers' app/src/main/java/io/github/yisus/nexo/AppDatabase.kt
sed -i '1i import androidx.compose.ui.graphics.asImageBitmap\nimport androidx.lifecycle.lifecycleScope' app/src/main/java/io/github/yisus/nexo/MainActivity.kt
sed -i 's/val paint1 = android.graphics.Paint().apply { color = android.graphics.Color.argb((0.15f\*255).toInt(), color1.red\*255, color1.green\*255, color1.blue\*255); isAntiAlias = true }/val paint1 = android.graphics.Paint().apply { color = android.graphics.Color.argb((0.15f*255).toInt(), (color1.red*255).toInt(), (color1.green*255).toInt(), (color1.blue*255).toInt()); isAntiAlias = true }/g' app/src/main/java/io/github/yisus/nexo/MainActivity.kt
sed -i 's/val paint2 = android.graphics.Paint().apply { color = android.graphics.Color.argb((0.15f\*255).toInt(), color2.red\*255, color2.green\*255, color2.blue\*255); isAntiAlias = true }/val paint2 = android.graphics.Paint().apply { color = android.graphics.Color.argb((0.15f*255).toInt(), (color2.red*255).toInt(), (color2.green*255).toInt(), (color2.blue*255).toInt()); isAntiAlias = true }/g' app/src/main/java/io/github/yisus/nexo/MainActivity.kt
sed -i 's/private fun checkPerformance()/private suspend fun checkPerformance()/g' app/src/main/java/io/github/yisus/nexo/PerformanceMonitor.kt
